interface HeaderProps {
	links: string[];
}

interface IntroCardProps {
	title: string;
	body: string;
	profiles: string[];
}

interface FooterProps {
	links: Array<string>;
}

export type { HeaderProps, IntroCardProps, FooterProps };
